RSpec.describe Majestic::Api::Lookup do
  it "has a version number" do
    expect(Majestic::Api::Lookup::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
